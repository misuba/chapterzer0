# Tests should be written in irb. That's where you will learn the
# methods well enough to recognize succeeding and failing
# behavior.

describe "posts of interest" do
	it "should have via:packrati.us posts"
	it "should not have private posts"
	describe "search" do
		it "should list top n tags in the search result"
	end
end

describe "tags" do
	it "should have a visualization from that third party link" 
	it "should respond to name, count"
end

describe "visualizations" do
	# graphiz
	# Golan
end
